name: Deploy to Jooper Server

on:
  push:
    branches:
      - main   # or your main branch name

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Copy files to server with SCP
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.JOOPER_SSH_KEY }}
          source: "."      # what to upload — whole repo here
          target: "/root/apps"  # where on your server

      - name: Run remote commands on server
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.JOOPER_SSH_KEY }}
          script: |
            cd /root/apps/ai-chatbot-backend
            git pull origin main
            npm i
            npx prisma migrate dev
            npx tsc
            pm2 restart all

